/* eslint-disable import/prefer-default-export */
export const defaultConfig = {
  coordinates: false,
  highlight: {
    lastMove: true,
    check: true,
  },
  animation: {
    enabled: true,
    duration: 250,
  },
};
