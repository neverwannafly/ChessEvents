import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    success: {
      main: '#359a6c',
    },
    danger: {
      main: '#a54a5c',
    },
  },
});

export default theme;
